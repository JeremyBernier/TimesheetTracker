import { css } from 'lit-element'

export default css`
  input, button {
    background-color: inherit;
    color: inherit;
    border-style: inherit;
    border-color: inherit;
    border-size: inherit;
    outline: none;
    cursor: pointer;
  }
`